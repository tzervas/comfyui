# Ollama Configuration
OLLAMA_PORT=11434
OLLAMA_ORIGINS=*
HTTPS_PROXY=
OLLAMA_DEBUG=false

# ComfyUI Configuration (use semver tags; switch to digests later if needed)
COMFYUI_IMAGE=ghcr.io/ai-dock/comfyui:0.3.0-cpu  # Change to matching -cuda tag for GPU
COMFYUI_PORT=8188
WEB_ENABLE_AUTH=true  # ai-dock Caddy/Service Portal auth gate (set false when nginx provides auth)
COMFYUI_EXTRA_ARGS=--auto-launch  # Additional args like --offline or --security-level strong
HF_TOKEN=your_huggingface_token_here  # For gated models; leave empty if not needed
COMFYUI_PATH=/workspace

# GPU Configuration (set GPU_DRIVER to 'nvidia' or 'amd' if available; 'cpu' for none)
GPU_DRIVER=cpu  # Set to nvidia if GPU available
GPU_COUNT=all

# Nginx Configuration
NGINX_PORT=8080
NGINX_SSL_PORT=8443
NGINX_BASIC_AUTH_USER=admin
NGINX_BASIC_AUTH_PASSWORD=change-me

# LAN DNS / Ingress
# Set this to your router DNS record (e.g., homelab.lan or ai.homelab.lan)
STACK_FQDN=homelab.lan

# Nginx upstream targets (Docker DNS defaults)
# For single-node deployments, leave these as container targets.
# For GPU-worker deployments, point these at the worker host:port.
OLLAMA_UPSTREAM=ollama:11434
COMFYUI_UPSTREAM=comfyui:18188

# Optional mTLS at ingress for registry endpoints (/register, /discover)
# Values: off | optional | on
MTLS_VERIFY=off

# LangChain Configuration
LANGCHAIN_PORT=8000

# LangFlow Configuration
LANGFLOW_PORT=7860

# Optional: Proxy timeouts or other Nginx tweaks (extend if needed)
# NGINX_CLIENT_MAX_BODY_SIZE=100M

# Quickstart Configuration
QUICKSTART_MODE=true  # Enable default workflows, flows, and user profiles

# Model Vault Configuration (Future Enhancement)
MODEL_VAULT_PORT=8080
# Protects Model Vault API endpoints (Bearer token or X-Model-Vault-Token)
MODEL_VAULT_TOKEN=your-secure-vault-token-here
MODEL_VAULT_URL=http://model-vault:8080
MODEL_VAULT_STORAGE_PATH=/var/lib/model-vault

# Service discovery / registry secret (protects /register, /discover)
REGISTRY_SECRET=your-secure-registry-secret-here