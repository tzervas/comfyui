# Local env overrides (never commit real env files)
.env*
!.env*.example

# Secrets / auth
.htpasswd
.credentials/

# TLS / mTLS material (generated locally)
ssl/

# Build outputs
target/

# Local caches / logs
.mypy_cache/
test-logs/
backups/
.sso-credentials-*.txt

# Chat/context handoff snapshots (local-only)
.context-snapshots/
